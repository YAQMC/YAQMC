# 安全策略

> **简体中文** | [English](SECURITY-EN.md)

## 私下报告漏洞

不要为未修复漏洞、可用凭据或真实账号数据创建公开 Issue。请使用 GitHub 的
[Private vulnerability reporting](https://github.com/YAQMC/YAQMC/security/advisories/new) 提交：

- 受影响版本与平台；
- 最小、无害的复现步骤；
- 预期与实际结果；
- 影响范围和需要的前置条件；
- 可选的修复建议。

请先移除 Cookie、OAuth code、token、vkey/ekey、签名 URL、真实 UIN、昵称、歌单名和完整响应体。若必须
提供敏感证据，请在私有报告中先描述类型与获取方式，等待维护者确认安全传输方法。

## 范围

优先处理：OAuth/state 验证绕过、秘密泄露、歌词 WebView 越权、路径穿越、任意文件/命令访问、本地 API
认证绕过、跨账号缓存污染、授权音源越权和更新/构建供应链问题。

不属于漏洞：腾讯兼容接口自身不稳定、会员/地区/版权限制按预期生效、Wayland 合成器限制、已知的同一 OS
用户恶意软件可以访问其自身凭据服务，以及没有安全影响的普通功能缺陷。

当前 `0.1.x` 测试版接收安全修复；更早的开发快照不保证回补。项目不承诺固定响应 SLA，但会在不扩大
暴露的前提下确认、分级和修复有效报告。
