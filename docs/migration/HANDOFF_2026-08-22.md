# Temporary maintainer handoff — 2026-08-22 (P14-C session)

**2026-08-22 overlay (do not rewrite the frozen body below):** the open items
in §5.1/§5.2 were executed by the maintainer. The soak waiver for `476b37e`
is recorded (`waivedBy: Osilvfe`, `waivedOn: 2026-08-22`,
`maintainer-authorized-skip`) and `npm run p14c:report` now prints
**READY** (18 targeted script tests pass). The working tree was committed in
three batches and pushed to `origin/feat/electron-migration`:

- `0c42fce` fix(provider): harden qmapi account/credential/transport production paths
- `49ae19a` docs(migration): record the 476b37e soak waiver and advance P14-C records
- `8d9fc67` docs: refresh third-party notices and contributing text for the linked dependency

Remaining for the next maintainer: **P15 only** (§5.3). The Linux
electron-builder script-test hang and the Node 24.19 engines pin remain as
described in §4/§5.5.

This snapshot summarizes the Codex working session
`01a0251f-1f98-7693-b0bf-c84e42034783` (worktree
`/home/osilvfe/Documents/dev/YAQMC`, sibling
`/home/osilvfe/Documents/dev/qm-api-rs`). The session ended mid-task
(usage limit) with 32 modified files **uncommitted**. This document is the
handover: what is already pushed, what is in the working tree, what is
verified, and what the next maintainer must decide or finish.

**Status vocabulary used (do not collapse these):**

| Status         | Meaning                                                                                          |
| -------------- | ------------------------------------------------------------------------------------------------ |
| **PASS-AUTO**  | Proven only by automated tests in this worktree. Not HUMAN acceptance.                           |
| **PASS-HUMAN** | Manually verified by the maintainer on a real `npm run dev:desktop` (or equivalent native) path. |
| **FAIL-HUMAN** | Manually reproduced failure.                                                                     |
| **NOT TESTED** | No HUMAN record. Do not infer PASS because code or automated tests exist.                        |
| **BLOCKED**    | External or process gate; not an implementation "green".                                         |

---

## 1. Executive state

| Field                    | Value                                                                                                                                    |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| YAQMC branch             | `feat/electron-migration`                                                                                                                 |
| YAQMC HEAD               | `9ed3537` `feat(provider): complete the P14-C cutover to the qmapi production backend`                                                    |
| Origin sync              | HEAD **equals** `origin/feat/electron-migration`. No unpushed local commits.                                                              |
| Working tree             | **NOT clean.** 32 modified files, ~2375 insertions / 394 deletions, **0 untracked**. All of it belongs to this session and is described in §3. |
| qm-api-rs branch         | `main`, **clean**, at `476b37e` `fix(models): tolerate missing vkey/ekey fields in clear vkey items`                                     |
| Current qm-api-rs pin    | `476b37e3135560dff132e9ba8996e068af706458` (working-tree Cargo.toml; committed tree still pins `ffcc86c…`)                                |
| Cutover baseline pin     | `ffcc86cec2993b79ccf34faf25c1eba6c0d995ca` (the pin actually cut over at `9ed3537`)                                                        |
| Migration phase          | **P14-C cutover complete.** `qmapi` is the production backend; in-tree L/I/H network fallbacks removed. P15 not started.                   |
| P14-C gate `exact-pin-three-day-soak` | **BLOCKED** for pin `476b37e`: the recorded waiver applies only to `ffcc86c`. `npm run p14c:report` correctly reports `BLOCKED`. |
| Maintainer authorization | In-session, the maintainer said gates may be recorded as **maintainer-authorized skips** ("门禁可以先标记我授权跳过") and asked for **batched commits** ("记得要分批提交"). Neither has been executed yet. |
| `main`                   | Still frozen. All work stays on `feat/electron-migration`.                                                                                 |
| Desktop app state        | Running at session end: core protocol=1 handshake OK, MPRIS ready, account `authenticated`.                                               |

**Read this first:**

- P14-C is functionally complete and verified (see §4). The only open P14-C
  gate is the exact-pin soak for `476b37e`.
- The maintainer authorized recording that gate as a waiver; the JSON/docs
  have **not** been updated yet, so the gate remains fail-closed `BLOCKED`.
- 32 files of uncommitted work must be committed in logical batches (§3)
  before or together with the waiver update.
- The session's own rules for approved command prefixes live in
  `~/.codex/rules/default.rules` (cargo/npm/git read-only + specific probes).

---

## 2. Already pushed (on origin, newest first)

| SHA       | Message                                                                      | Content                                                                                          |
| --------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `9ed3537` | feat(provider): complete the P14-C cutover to the qmapi production backend   | Unconditional `qqmusic-api` at `ffcc86c`; L/I/H in-tree network fallbacks removed; qmapi default. |
| `285b126` | docs(migration): record the maintainer waiver of the three-day soak          | Waiver for the `ffcc86c` cutover baseline only.                                                  |
| `dc4db82` | feat(migration): close the crate-provenance gate at qm-api-rs pin `ffcc86c`  | Independent-implementation record; L-1124 port claims removed.                                   |
| `11df50c` | docs(migration): close the P14-B live-hybrid gate on maintainer re-verification |                                                                                                  |
| `7cca09f` | feat(provider): route production QMC decryption through the library adapter  |                                                                                                  |
| `aa93b3d` | test(provider): add the dual-path real-file QMC golden harness                |                                                                                                  |
| `f73596d` | chore(ci): add P14-C readiness report and enforcement scripts                 |                                                                                                  |
| `375c9d7` | docs(migration): record P14-B/P14-C gates and the current qm-api-rs pin       |                                                                                                  |
| `b54495c` | ci: authenticate the private qm-api-rs pin and exercise the qmapi feature     |                                                                                                  |
| `7a1bc46` | feat(provider): add the optional qmapi backend at the qm-api-rs pin           |                                                                                                  |

qm-api-rs side (already pushed to its `main`):

| SHA       | Message                                                        |
| --------- | -------------------------------------------------------------- |
| `476b37e` | fix(models): tolerate missing vkey/ekey fields in clear vkey items |
| `ffcc86c` | docs: record independent implementation and drop the L-1124 port claims |

---

## 3. Uncommitted working tree — proposed commit batches

All diffs were produced by this session. Suggested batch order (user asked
for batched commits; do not squash into one commit):

### Batch 1 — `fix(provider): harden qmapi account/credential/transport production paths`

Rust code + tests + pin change:

| File                                                          | Change                                                                                                                             |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `crates/yaqmc-provider-qqmusic/Cargo.toml`, `Cargo.lock`      | Pin `ffcc86c…` → `476b37e…` (clear-vkey tolerant parsing).                                                                          |
| `…/src/qmapi/credential.rs` (+535)                            | Credential refresh transaction: validate-before-write, original-value snapshot, write, read-back, byte-for-byte rollback on failure; dual-slot (legacy `qqmusic-session` + `qqmusic-credential-v2`) sync; scope consistency across runtime/legacy/v2; transient network errors never misreported as expired lease. |
| `…/src/qmapi/account.rs` (+360), `…/src/qqmusic/account.rs` (+391) | Account write path keeps the mobile identity envelope (`authst/uid/qq/loginUin/tmeLoginType`, `ct/cv/v`) that the library default web `comm` dropped (root cause of `80105` on favorite remove); `80105` + `retCode=0` classified as OutcomeUnknown → read-only reconciliation, never false success; read-fail never mis-confirmed as "playlist gone"; concurrent projection lock. |
| `…/src/qmapi/transport.rs` (+78)                              | Cross-origin 307/308 redirects do not forward credential-bearing bodies; only exact post-send decode stages mark OutcomeUnknown.   |
| `…/src/qmapi/vkey.rs` (+124)                                  | `RS02` official-trial file type maps to `SpecialSongFileType::Try` instead of aborting the whole clear-vkey batch with MalformedResponse (playback root cause). |
| `…/src/qmapi/lyric.rs` (+29), `entitlement.rs` (+26)          | Log redaction: credential/vkey/entitlement/lyric paths no longer log upstream raw error payloads (synthetic `authst`/`qqmusic_key`/`cookie` leak regressions covered). |
| `…/src/qqmusic/auth.rs` (+448), `…/src/qqmusic.rs` (+10)      | Auth/account integration for the above.                                                                                             |
| `crates/yaqmc-core/tests/qa_play01_production.rs` (+32)       | LIVE test now unconditionally restores the flipped favorite before checking the result; no leftover account state on assertion failure. |

### Batch 2 — `docs(migration): advance P14-C records to pin 476b37e and make the soak gate fail-closed`

Governance + machine-readable records + CI scripts:

| File                                                     | Change                                                                                                                  |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `docs/migration/p14c-readiness.json`                     | `targetPin` → `476b37e…`; new `cutoverBaselinePin` `ffcc86c…`; soak waiver now carries `appliesToPin: ffcc86c…`.         |
| `scripts/ci/p14c-readiness.mjs` (+71)                    | Soak gate fail-closed on `appliesToPin` mismatch; post-cutover pin reviews ignore historical blockers except the soak gate; tamper-proof checks (immutable 40-char SHA, exactly one soak gate, evidence required). |
| `scripts/ci/p14c-readiness.test.mjs` (+130)              | 10 targeted tests: old `ffcc86c` waiver shows `BLOCKED` for `476b37e`; deletion/duplication/fake-pass/missing fields cannot bypass. |
| `scripts/ci/qm-api-rs-access.mjs` (+2) / `.test.mjs` (+2)| Pin constant updated to `476b37e…`.                                                                                      |
| `docs/migration/p14c-readiness.md`, `p14b-live-verify.md`, `p14b-qmapi-backend.md`, `p14c-implementation.md`, `p14-qm-api-rs-provenance.md`, `p14-qm-api-rs-upstream.md`, `p14-entry-gates.md`, `governance.md`, `plan-deltas.md`, `provenance-audit.md`, `provenance-ledger.json` | Pin advancement, "P14 overlay: qm-api-rs pin 476b37e" delta entry, cutover wording moved to past tense, ledger `linked: true` with pin-specific QMCDecode sha256 evidence while `auditHead` stays frozen. |

Note: the docs in this batch currently say the waiver "was not reissued for
`476b37e`". If the maintainer's skip authorization is executed (§5.1), those
sentences and the JSON waiver must be updated together in the same batch.

### Batch 3 — `docs: refresh third-party notices and contributing text for the linked dependency`

| File                                             | Change                                                              |
| ------------------------------------------------ | ------------------------------------------------------------------- |
| `THIRD_PARTY_NOTICES.md` (+19/-8)                | Linked qm-api-rs overlay row; QMCDecode mapping retargeted to `qm-api-rs@476b37e` adapter path with pin-specific sha256. |
| `CONTRIBUTING.md`, `CONTRIBUTING-EN.md`          | Private-dep access wording for the current pin.                     |
| `CORRESPONDING_SOURCE_POLICY.md`                 | Same-pin alignment.                                                 |

---

## 4. Verification evidence recorded this session (all PASS-AUTO unless noted)

- Rust provider: **236 passed / 7 ignored** (ignored = LIVE-only). Full
  workspace test run: **0 failures**, including production QA
  `production_core_qq_live_or_blocked_login`.
- `cargo fmt --all` clean; Clippy `-D warnings` clean.
- Frontend: **80 test files, 623 tests green** (Vitest). Trap: this machine
  runs Node 26.7 (repo engines pin 24.19.0); Node 26's experimental Web
  Storage breaks jsdom — disable it to run tests, do not change product code.
- CI scripts: **167 of 168 passed** with the external-permission override.
  The one excluded case is the Linux packaging "dry-run" test which actually
  invokes `electron-builder --linux AppImage deb rpm tar.gz --x64` via
  `spawnSync` and hangs without output. It was stopped intentionally; it is a
  pre-existing test-design issue, not a product failure. Investigate
  separately if P15 requires it.
- **PASS-HUMAN / LIVE** (real QQ account, real network):
  - Guest home song resolves to playable `standard-mp3` via qm-api-rs.
  - Logged-in `lossless-mflac` with `evkey` through the qm-api-rs encrypted path; QMC RC4 and map decryptors both loaded in desktop logs; one seek settled at 34.353 s.
  - Lyric: 36 lines parsed through qm-api-rs production path.
  - Favorite remove/restore LIVE round trip confirmed (see root-cause notes below).
  - Desktop restart: protocol=1 handshake, MPRIS ready, account restored `authenticated`.
- Root causes proven and fixed this session:
  - Lyric `24001` was probe MIDs, not a protocol bug (real MID returns code 0).
  - Clear playback failure = `RS02` mapping gap (fixed in-tree; library side hardened by `476b37e`).
  - Favorite remove `80105` = library default web `comm` missing mobile identity fields (fixed by explicit verified `comm`); `songtype` DTO extension was tested then **reverted** as unnecessary.
  - Old keyring credential is server-side invalid (official check returns `1000`); re-login (QR) is required and was completed during the session.

---

## 5. Open items for the next maintainer

### 5.1 Close the `476b37e` soak gate (maintainer decision recorded, not executed)

In-session maintainer authorization: gates may be recorded as
**maintainer-authorized skips**; goal mode asked to "drive P14-C/P15 to
completion with all gates green".

To close, in one batch:

1. `docs/migration/p14c-readiness.json`: set the
   `exact-pin-three-day-soak` waiver `appliesToPin` to
   `476b37e3135560dff132e9ba8996e068af706458` (or add a second waiver entry).
2. `docs/migration/p14c-readiness.md`: update the waiver paragraph and the
   "was not reissued" wording; record the authorization date and that it is
   an explicit maintainer skip, not soak evidence.
3. Run `npm run p14c:report` and `npm run p14c:enforce`; expect `READY`.
4. Commit (see batches above) and push.

Do **not** mark the gate `pass` — only `waived`, with the maintainer name and
date. Do not fabricate soak evidence.

### 5.2 Commit and push the working tree in batches

Batch plan in §3. Commits must keep `main` untouched and stay on
`feat/electron-migration`. After pushing, re-run `npm run p14c:report` from a
clean tree to confirm the gate state matches what was pushed.

### 5.3 P15 — not started (plan §40)

Scope after P14-C closes: §38.3 doc updates, TD-7/TD-9 stale-doc fixes,
CONTRIBUTING (private-dep auth, new build), README badges/instructions,
decide delete-vs-promote for `docs/migration/` scratch, file follow-up
issues (signing R-9, notifications, deep links, native-Wayland, TD-3 DTO
cleanup), final full acceptance re-run (§46), exit checklist §47, final tag
`electron-migration-complete`. Checkpoint `CHECK-15`.

### 5.4 qm-api-rs provenance overlay

`provenance-ledger.json` now records `linked: true` at `476b37e` with
pin-specific sha256 evidence for the QMCDecode rewrite mappings, while
`auditHead` stays frozen (the in-tree snapshot was not re-audited). The
overlay note states exactly that. Do not widen claims to a full re-audit
unless one is actually performed.

### 5.5 Environment traps (for any follow-up session)

- Private git dependency fetch needs
  `CARGO_NET_GIT_FETCH_WITH_CLI=true` (already in `~/.codex/rules/default.rules`).
- Node must match engines `24.19.0`; Node 26 breaks jsdom tests (§4).
- The Linux `electron-builder` script test hangs in `spawnSync`; exclude or
  give it a hard timeout when running `npm run ci:test-scripts`.
- Legacy `qqmusic-session` keyring slot is kept as the bounded migration
  backup; delete only after validated cross-release migration, never merely
  because the v2 slot exists.
- Do not treat the running desktop instance as evidence for future runs;
  always confirm `host.log` `supervisor start binary=...` before debugging
  protocol mismatches.

---

## 6. Where the authoritative records live

- Machine-readable gate state: `docs/migration/p14c-readiness.json`
  (`npm run p14c:report` / `p14c:enforce`).
- Provenance: `docs/migration/provenance-ledger.json` +
  `provenance-audit.md` + `p14-qm-api-rs-provenance.md`.
- LIVE evidence: `docs/migration/p14b-live-verify.md`,
  `p14c-implementation.md`.
- Plan deltas: `docs/migration/plan-deltas.md` (new overlay entry
  "P14 overlay: qm-api-rs pin `476b37e` (2026-08-22)").
- Previous handoff (older phases): `docs/migration/HANDOFF_2026-08-18.md`.
