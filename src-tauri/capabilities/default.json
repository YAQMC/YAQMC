{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the main application and dedicated lyric surfaces",
  "windows": ["main", "lyrics-desktop", "lyrics-island"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
